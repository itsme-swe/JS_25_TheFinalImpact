function calculateCartPrice(...num1) {
  return num1;
}

let totalCartValue = calculateCartPrice(200, 200, 400, 500);

console.log(totalCartValue); // o/p: [ 200, 200, 400, 500 ]

/*

🔸 Rest Operator: The rest operator (...) in JavaScript is used to gather the rest of the elements or properties into a new array or object. It's especially useful when working with functions, arrays, and objects.

*/
