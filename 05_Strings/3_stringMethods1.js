let city = "Jaipur";

let city2 = "Ajmer";

let country = "japan";

console.log(city.length); // To find the length of string

console.log(city2.toUpperCase()); // Will convert all lower case to upper cases.

console.log(city.toLowerCase()); // Will convert all upper case to lower cases.

console.log(city.charAt(3)); // Will return the character present at indexValue

console.log(country.indexOf("a")); // Will return the indexValue of first occurence so here it will return 1 bcoz first 'a' is present at indexValue 1

console.log(country.lastIndexOf("a")); // Now it will return indexValue 3
