let msg = "   Hello, World!  ";

let greetMsg = "Namaste Duniya";

console.log(msg.trim()); // Will remove the white spaces from both the sides.

console.log(greetMsg.replace("Duniya", "World")); // Replaces a specified value with another value in a string. It only replaces the first occurrence.
